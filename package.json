{
  "name": "fdca-pools",
  "version": "1.0.0",
  "description": "FDCA Fire & Rescue Aquatic Centers – Pool scheduling site",
  "scripts": {
    "build": "esbuild src/main.ts --bundle --outfile=dist/bundle.js --platform=browser --target=es2020",
    "dev": "esbuild src/main.ts --bundle --outfile=dist/bundle.js --platform=browser --target=es2020 --watch",
    "serve": "npx serve ."
  },
  "devDependencies": {
    "esbuild": "^0.20.2",
    "typescript": "^5.4.2"
  }
}
